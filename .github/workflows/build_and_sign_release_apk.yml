name: 'Build Kiwi Reborn APK'

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-22.04
    timeout-minutes: 360

    steps:
      - name: Free up disk space
        run: |
          sudo rm -rf /usr/share/dotnet
          sudo rm -rf /usr/local/lib/android
          sudo rm -rf /opt/ghc
          df -h

      - name: Checkout your patches
        uses: actions/checkout@v4
        with:
          ref: kiwi
          path: kiwi-patches

      - name: Install depot_tools
        run: |
          git clone https://chromium.googlesource.com/chromium/tools/depot_tools.git $HOME/depot_tools
          echo "$HOME/depot_tools" >> $GITHUB_PATH

      - name: Read Chromium version
        id: ver
        run: |
          source kiwi-patches/CHROMIUM_VERSION
          echo "version=${CHROMIUM_MAJOR}.${CHROMIUM_MINOR}.${CHROMIUM_BUILD}.${CHROMIUM_PATCH}" >> $GITHUB_OUTPUT
          echo "major=$CHROMIUM_MAJOR" >> $GITHUB_OUTPUT

      - name: Configure gclient for Android
        run: |
          mkdir chromium && cd chromium
          gclient config --name=src \
            https://chromium.googlesource.com/chromium/src.git \
            --unmanaged
          echo "target_os = ['android']" >> .gclient

      - name: Fetch Chromium at pinned version
        run: |
          cd chromium
          gclient sync \
            --no-history \
            --nohooks \
            --revision=src@${{ steps.ver.outputs.version }} \
            -D

      - name: Apply Kiwi patches on top of Chromium
        run: |
          for dir in base chrome components content extensions net remoting services third_party ui; do
            if [ -d "kiwi-patches/$dir" ]; then
              echo "Applying $dir patches..."
              cp -rf kiwi-patches/$dir/* chromium/src/$dir/ 2>/dev/null || true
            fi
          done

      - name: Run gclient hooks
        run: |
          cd chromium
          gclient runhooks

      - name: Install Android build dependencies
        run: |
          cd chromium/src
          sudo ./build/install-build-deps.sh --android --no-prompt

      - name: Generate build files (arm64)
        run: |
          cd chromium/src
          gn gen out/arm64 --args='
            target_os="android"
            target_cpu="arm64"
            is_debug=false
            is_component_build=false
            is_official_build=false
            ffmpeg_branding="Chrome"
            proprietary_codecs=true
            android_channel="stable"
            chrome_pgo_phase=0
            
            # Fixes for Chromium 112+
            enable_updater=false
            enable_chrome_update=false
            enable_update_notifications=false
          '

      - name: Compile APK
        run: |
          cd chromium/src
          autoninja -C out/arm64 chrome_public_apk

      - name: Upload APK as artifact
        uses: actions/upload-artifact@v4
        with:
          name: kiwi-reborn-arm64
          path: chromium/src/out/arm64/apks/ChromePublic.apk
          retention-days: 7
